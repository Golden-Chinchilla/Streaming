/**
 * Plugin Registry Entry Point
 *
 * Import this file in your app's root layout or entry point to ensure
 * all diagram plugins are registered before the app starts.
 */

import "./sankey";
import "./swimlane";
